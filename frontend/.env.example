# Base URL for the backend API
# If hosted on separate servers, provide absolute URL (e.g. https://api.yourdomain.com/api)
# In development or if proxied on the same server, leave empty or use /api
VITE_API_URL=https://attendence-erp-1.onrender.com/api
